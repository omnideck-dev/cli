FROM docker.io/library/busybox:1.37.0

RUN mkdir -p /www /home/omnideck /var/lib/omnideck
COPY index.html /www/index.html

EXPOSE 8080

CMD ["sh", "-c", "echo omnideck-hardware-fixture-started; exec httpd -f -p 8080 -h /www"]
